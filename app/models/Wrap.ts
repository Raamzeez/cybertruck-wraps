interface Wrap {
  _id: string;
  title: string;
  image: string;
  author: string;
  description?: string;
  official?: boolean;
  profilePicture?: string;
  anonymous?: boolean;
  isAuthor?: boolean;
}

export default Wrap;
